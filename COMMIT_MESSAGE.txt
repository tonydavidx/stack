feat: enhance PWA robustness and database initialization

- Refactor `db.svelte.ts` to use a lazy, browser-only PouchDB initialization pattern
- Add explicit service worker registration and web manifest links to `app.html`
- Update `vite.config.ts` with explicit PWA `id`, `purpose`, and `injectRegister` settings
- Implement safer database access across all CRUD operations with `getDbInstance`
- Simplify root layout by removing redundant `pwaInfo` handling
- Track item `usageCount` for frequency-based sorting in history
- Add development tools (seed/clear data) to Sidebar (dev mode only)
