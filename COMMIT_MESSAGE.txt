feat: add item frequency tracking and development tools

- Implement `usageCount` on `ItemDoc` to track frequency of item additions
- Sort history modal by `usageCount` (descending) for quicker re-selection
- Add `Seed Data` and `Clear All` development controls to Sidebar (visible in dev mode only)
- Integrate `virtual:pwa-info` in root layout for improved PWA metadata handling
- Update `app.html` with improved formatting and metadata structure
- Fix database item addition logic to increment usage counts correctly
