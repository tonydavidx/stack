feat: implement robust DB initialization and refine PWA config

- Introduce `initPromise` and `ensureInit` in `db.svelte.ts` to prevent race conditions during DB setup
- Ensure all CRUD operations wait for database initialization before execution
- Switch PWA manifest and service worker paths to relative in `app.html` for better deployment compatibility
- Refine `package.json` preview script to explicitly use the `build` directory
- Improve database error handling during the initialization phase
- Track item usage frequency for prioritized history sorting
- Add development tools (seed/clear data) with safer async handling
